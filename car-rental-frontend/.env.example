# Backend API Configuration
# The base URL for the backend API (without /api suffix)
#
# IMPORTANT:
# - Leave EMPTY for local development (uses Vite proxy, avoids CORS issues)
# - Set to full URL for production build (e.g., https://carflow-reservation-system.onrender.com)
#
# Development (uses Vite proxy):
VITE_API_BASE_URL=
#
# Production (uses direct connection):
# VITE_API_BASE_URL=https://carflow-reservation-system.onrender.com

# Tenant Email
# The email address that identifies your tenant in the multi-tenant system
VITE_TENANT_EMAIL=lerent@lerent.sk

# Development Mode
# Set to 'true' to use mock data (offline development)
# Set to 'false' to connect to real backend API
VITE_USE_MOCK_DATA=false
